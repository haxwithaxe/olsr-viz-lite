Put on a machine running olsrd and run "self_serve.py" or put it in the webroot of a webserver and make the "cgi-bin" directory a cgi-bin.
To set the port edit "self_serve.py" or set the "PORT" environmental variable to any integer between 1 and 65335 (given sufficient permissions for 1 to 1024).
